[profile.release]
lto = true
incremental = true

[profile.bench]
debug = true

[workspace]
members = [
    "libraries/benchmarks",
    "libraries/ldd-library/benchmarks", # Add these crates separatedly since the ldd-library has it's own workspace
    "libraries/ldd-library/ldd",
    "libraries/mcrl2-rust",
    "libraries/rec_tests",
    "libraries/sabre",
    "tools/*"
]